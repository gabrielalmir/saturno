services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ..:/workspaces/saturno:cached
    command: sleep infinity
    environment:
      DB_CONNECTION: sqlite
      DB_DATABASE: /workspaces/saturno/database/database.sqlite
    env_file:
      - path: ../.env
        required: false
